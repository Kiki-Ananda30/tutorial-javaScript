/* let sayHello = "hello world!";
let H = sayHello[0].toUpperCase();
console.log(H);
let W = sayHello[6].toUpperCase();
console.log(W);

let theRest = sayHello.slice(1, 5);
console.log(theRest);
let theRest1 = sayHello.slice(7);
console.log(theRest1);

// let greetings = H + theRest + " " + W + theRest1; // ⁡⁢⁢⁢or⁡
let greetings = `${H}${theRest} ${W}${theRest1}`;
console.log(greetings); */

// ⁡⁢⁣⁣Array di javascript⁡

// ⁡⁢⁣⁣Apa itu array?, ⁡⁣⁣⁡⁣⁣⁢array adalah struktur data yang digunakan untuk menyimpan koleksi elemen, seperti angka atau string dalam satu variabel. Di javascript array dapat menyimpan berbagai tipe data sekaligus dan memiliki indeks yang dimulai dari 0.⁡

// ⁡⁣⁣⁢Membuat array⁡

// ⁡⁣⁢⁣1. Menggunakan notasi array literal:⁡
